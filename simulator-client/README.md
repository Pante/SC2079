# Algo Simulator

This simulator uses Next.js. It communicates with the Flask service (in the service folder) to simulate pathfinding.

## Setup:

Ensure you are in the simulator-client directory:
```shell
cd simulator-client
```

Install dependencies:
```shell
npm i
```

Run the script:
```shell
npm run dev
```

The simulator should be running on http://localhost:3000


# mdp_api

MdpApi - JavaScript client for mdp_api
No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
This SDK is automatically generated by the [OpenAPI Generator](https://openapi-generator.tech) project:

- API version: 1.0.0
- Package version: 1.0.0
- Build package: org.openapitools.codegen.languages.JavascriptClientCodegen

## Installation

### For [Node.js](https://nodejs.org/)

#### npm

To publish the library as a [npm](https://www.npmjs.com/), please follow the procedure in ["Publishing npm packages"](https://docs.npmjs.com/getting-started/publishing-npm-packages).

Then install it via:

```shell
npm install mdp_api --save
```

Finally, you need to build the module:

```shell
npm run build
```

##### Local development

To use the library locally without publishing to a remote npm registry, first install the dependencies by changing into the directory containing `package.json` (and this README). Let's call this `JAVASCRIPT_CLIENT_DIR`. Then run:

```shell
npm install
```

Next, [link](https://docs.npmjs.com/cli/link) it globally in npm with the following, also from `JAVASCRIPT_CLIENT_DIR`:

```shell
npm link
```

To use the link you just defined in your project, switch to the directory you want to use your mdp_api from, and run:

```shell
npm link /path/to/<JAVASCRIPT_CLIENT_DIR>
```

Finally, you need to build the module:

```shell
npm run build
```

#### git

If the library is hosted at a git repository, e.g.https://github.com/GIT_USER_ID/GIT_REPO_ID
then install it via:

```shell
    npm install GIT_USER_ID/GIT_REPO_ID --save
```

### For browser

The library also works in the browser environment via npm and [browserify](http://browserify.org/). After following
the above steps with Node.js and installing browserify with `npm install -g browserify`,
perform the following (assuming *main.js* is your entry file):

```shell
browserify main.js > bundle.js
```

Then include *bundle.js* in the HTML pages.

### Webpack Configuration

Using Webpack you may encounter the following error: "Module not found: Error:
Cannot resolve module", most certainly you should disable AMD loader. Add/merge
the following section to your webpack config:

```javascript
module: {
  rules: [
    {
      parser: {
        amd: false
      }
    }
  ]
}
```

## Getting Started

Please follow the [installation](#installation) instruction and execute the following JS code:

```javascript
var MdpApi = require('mdp_api');


var api = new MdpApi.ImageRecognitionApi()
var file = "/path/to/file"; // {File} 
var callback = function(error, data, response) {
  if (error) {
    console.error(error);
  } else {
    console.log('API called successfully. Returned data: ' + data);
  }
};
api.imagePredictionTask1Post(file, callback);

```

## Documentation for API Endpoints

All URIs are relative to *http://localhost*

Class | Method | HTTP request | Description
------------ | ------------- | ------------- | -------------
*MdpApi.ImageRecognitionApi* | [**imagePredictionTask1Post**](docs/ImageRecognitionApi.md#imagePredictionTask1Post) | **POST** /image/prediction/task-1 | 
*MdpApi.ImageRecognitionApi* | [**imagePredictionTask2Post**](docs/ImageRecognitionApi.md#imagePredictionTask2Post) | **POST** /image/prediction/task-2 | 
*MdpApi.ImageRecognitionApi* | [**imageStitchPost**](docs/ImageRecognitionApi.md#imageStitchPost) | **POST** /image/stitch | 
*MdpApi.PathfindingApi* | [**pathfindingPost**](docs/PathfindingApi.md#pathfindingPost) | **POST** /pathfinding/ | 


## Documentation for Models

 - [MdpApi.Cost](docs/Cost.md)
 - [MdpApi.Direction](docs/Direction.md)
 - [MdpApi.ErrorContext](docs/ErrorContext.md)
 - [MdpApi.ErrorType](docs/ErrorType.md)
 - [MdpApi.ImagePredictionResponse](docs/ImagePredictionResponse.md)
 - [MdpApi.Location](docs/Location.md)
 - [MdpApi.Message](docs/Message.md)
 - [MdpApi.MiscInstruction](docs/MiscInstruction.md)
 - [MdpApi.Move](docs/Move.md)
 - [MdpApi.Path](docs/Path.md)
 - [MdpApi.PathfindingPoint](docs/PathfindingPoint.md)
 - [MdpApi.PathfindingRequest](docs/PathfindingRequest.md)
 - [MdpApi.PathfindingRequestObstacle](docs/PathfindingRequestObstacle.md)
 - [MdpApi.PathfindingRequestRobot](docs/PathfindingRequestRobot.md)
 - [MdpApi.PathfindingResponse](docs/PathfindingResponse.md)
 - [MdpApi.PathfindingResponseMoveInstruction](docs/PathfindingResponseMoveInstruction.md)
 - [MdpApi.PathfindingResponseSegment](docs/PathfindingResponseSegment.md)
 - [MdpApi.PathfindingResponseSegmentInstructionsInner](docs/PathfindingResponseSegmentInstructionsInner.md)
 - [MdpApi.PathfindingVector](docs/PathfindingVector.md)
 - [MdpApi.TurnInstruction](docs/TurnInstruction.md)
 - [MdpApi.ValidationErrorModel](docs/ValidationErrorModel.md)


## Documentation for Authorization

Endpoints do not require authorization.

